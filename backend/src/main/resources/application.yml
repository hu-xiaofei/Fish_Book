spring:
  application:
    name: fishbook-backend
  jackson:
    time-zone: UTC
  jpa:
    open-in-view: false
  session:
    jdbc:
      initialize-schema: never

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
